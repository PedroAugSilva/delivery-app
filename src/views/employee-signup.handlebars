<main class="h-screen w-screen grid place-items-center bg-zinc-100">
  <div
    class="flex flex-col items-center gap-2 w-72 h-max p-4 bg-white rounded-md shadow-md shadow-black/10"
  >
    <h1
      class="block text-2xl font-bold text-zinc-500 leading-tight my-2"
    >Cadastrar Funcionário</h1>
    <form
      class="w-full flex flex-col items-center gap-2"
      method="post"
      action="/user"
    >
      <input
        type="username"
        placeholder="Nome do funcionário..."
        name="employeename"
        required
        class="w-full h-8 rounded-md px-2 text-black outline-none border-none ring-1 focus:ring-2 ring-black"
      />
      <input
        type="text"
        placeholder="Chave de acesso..."
        name="acesskey"
        required
        class="w-full h-8 rounded-md px-2 text-black outline-none border-none ring-1 focus:ring-2 ring-black"
      />
      <select name="selectrole">
        <option value="1">Entregador</option> 

      </select>
      <button
        class="w-full bg-black items-center justify-center flex h-8 rounded-md text-white font-semibold"
      >
        Cadastrar
      </button>
    </form>

  </div>
</main>